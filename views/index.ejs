<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bitcoin 24h Price Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="/styles/main.css" rel="stylesheet">
</head>
<body>
    <div class="px-4 py-5 my-5 text-center">
        <h1 class="display-5 fw-bold text-body-emphasis">
            <% if (data.quotes["USD"].market_cap_change_24h<0){ %>
                Diamond hands, keep the faith, no FUD, etc.
            <%} else {%>
                The line goes up!!!
            <% } %>
        </h1>
        <% if (data.quotes["USD"].market_cap_change_24h<0){ %>
            <img class="d-block mx-auto mb-4" src="/images/diamond hands.jpg" alt="Diamond hands">
        <%} else {%>
            <img class="d-block mx-auto mb-4" src="/images/the line goes up.jpg" alt="Graph of a line going up">
        <% } %>
        <h3 class="fw-normal text-muted mb-3">
            <% if (data.quotes["USD"].market_cap_change_24h>0){ %>
                <%= data.name %> is up <%= data.quotes["USD"].market_cap_change_24h %>% in the past 24 hours.
            <% } else{ %>
                <%= data.name %> is down <%= data.quotes["USD"].market_cap_change_24h*(-1) %>% in the past 24 hours.
            <% } %>
          </h3>
        <div class="col-lg-6 mx-auto">
          <p class="lead mb-4">The current price of <%= data.name %> is $<%=data.quotes["USD"].price %>.</p>
        </div>
    </div>
    <!-- This footer is from my last project -->
    <footer class="footer mt-auto py-3 bg-body-tertiary">
        <div class="container bg-body-tertiary">
          <span class="text-body-secondary">© Alex Yuan 2023</span>
        </div>
    </footer>
</body>
</html>