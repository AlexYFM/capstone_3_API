{
  "name": "capstone_3_api",
  "version": "1.0.0",
  "description": "To run this repo, just clone it, use \"npm install\" in the base directory, and run \"nodemon index.js\" to start the app in localhost:3000.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Yuan",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2"
  }
}
